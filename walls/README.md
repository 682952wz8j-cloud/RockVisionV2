# Wall Packages

Validated final packages only.

`BundleWallDataProvider` (and later cache / remote providers) load from
here — not from `incoming/` or `offline/work/`.

Produced by:

```text
incoming/  →  offline/work/  →  walls/
```
