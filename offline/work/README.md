# Offline work

Generated intermediates only.

Examples: Gate 1A `ingestion/inventory.json` and
`ingestion/validation_report.md`, later COLMAP / OpenCV intermediates.

This folder is reproducible. It may be deleted and rebuilt.

Do not place Chairman source files here. Those belong in `incoming/`.
Do not treat this folder as a Wall Package. Those belong in `walls/`.
